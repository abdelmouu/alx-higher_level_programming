#!/usr/bin/python3
def multiple_returns(sentence):
    new.tuple = ()
    if len(sentence) == 0:
        new.tuple = 0, "None"
    else:
        new.tuple = len(sentence), sentence[0]
    return new.tuple
